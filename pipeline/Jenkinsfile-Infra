@Library('dotnet-shared-library') _
pipeline {
  agent any
  environment {
    azcon = credentials('azure-pasha')
  }
   
   stages {   
    
    stage('Infra Provisioning/Destroying') {
      steps {
        TerraformDestroy()
      }
    }
	}
	}
